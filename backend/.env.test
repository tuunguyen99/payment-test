# Test environment — PostgreSQL via Docker (docker-compose -f docker-compose.test.yml up -d)
# Container exposes port 5433 → maps to localhost:5433
NODE_ENV=test
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secret
POSTGRES_DB=projectz_test
